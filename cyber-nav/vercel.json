{
    "framework": "nextjs",
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.py"
        }
    ],
    "buildCommand": "npm run build",
    "outputDirectory": ".next",
    "installCommand": "npm install"
}